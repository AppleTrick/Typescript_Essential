let age = 10; // 10을 나이로 유추..
let x = 10; // 10의 데이터가 불분명

let weight:number = 80;

type Centimeter = number;
let height: Centimeter = 176;
// 코드상에서 유형으로 표현 가능

type RainbowColor = 'red' | 'orange' | 'yellow' | 'blue';
let color : RainbowColor = 'orange';